git使用：
git config --global user.name '291390412'
git config --global user.email '291390412@qq.com'
git config -l   检查配置
git status  查看本地变化
git init 
git add *  
git commit -m "first commit"     上传文件，截至本地修改生效了。
git branch -M main
git remote add origin https://github.com/291390412/test-frame.git
git push -u origin main    开始推送远端

弹出弹窗 personal access token关闭，然后在CMD中输入usname 和 password即可